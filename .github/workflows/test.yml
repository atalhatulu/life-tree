name: tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node --test tests/calibration_a.test.js tests/group3_part1.test.js
      - run: npm run fast-sim -- --lives 500 --to-age 100 --policy human-like --seed-prefix calibration-a
