name: tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node --test tests/calibration_b.test.js tests/relationship_family_v2.test.js
      - run: npm run fast-sim -- --lives 500 --to-age 100 --policy human-like --seed-prefix calibration-b
